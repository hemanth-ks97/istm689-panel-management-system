import React from "react";

// MUI
import { Box, Typography } from "@mui/material";

const AdminPage = () => {
  return (
    <Box sx={{ flexGrow: 1 }}>
      <Typography variant="h4">This is the AdminPage component.</Typography>
    </Box>
  );
};

export default AdminPage;
