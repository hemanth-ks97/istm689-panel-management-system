import React from "react";

// MUI
import { Box, Typography } from "@mui/material";

const PanelPage = () => {
  return (
    <Box sx={{ flexGrow: 1 }}>
      <Typography variant="h4">This is where Dr Gomillion will add panel information like start date, end date, topic etc</Typography>
    </Box>
  );
};

export default PanelPage;
