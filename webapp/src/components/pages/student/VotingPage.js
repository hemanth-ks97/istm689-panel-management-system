import React from "react";

// MUI
import { Box, Typography } from "@mui/material";

const VotingPage = () => {
  return (
    <Box sx={{ flexGrow: 1 }}>
      <Typography variant="h4">This is the VotingPage component.</Typography>
      <Typography>Placeholder</Typography>
    </Box>
  );
};

export default VotingPage;
