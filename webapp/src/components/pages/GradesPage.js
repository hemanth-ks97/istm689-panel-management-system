import React from "react";

// MUI
import { Box, Typography } from "@mui/material";

const GradesPage = () => {
  return (
    <Box sx={{ flexGrow: 1 }}>
      <Typography variant="h4">This is the GradesPage component.</Typography>
      <Typography>Placeholder</Typography>
    </Box>
  );
};

export default GradesPage;
