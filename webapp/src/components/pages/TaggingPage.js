import React from "react";

// MUI
import { Box, Typography } from "@mui/material";

const TaggingPage = () => {
  return (
    <Box sx={{ flexGrow: 1 }}>
      <Typography variant="h4">This is the TaggingPage component.</Typography>
      <Typography>Placeholder</Typography>
    </Box>
  );
};

export default TaggingPage;
